We have created cli with api also
Now in cli we need few feature

1. pyagenity build -> generate docker compose
2. pyagenity server api
3. pyagenity web -> it will open react app also


What App will do?
1. What ever api is served by the backend will be consumed by the frontend
2. It will be quick prototype for developer, to test the graph
3. State need to be bi-directional (Frontend <-> Backend)
4. Message should support stream and invoke both (From frontend we can change this settings)
5. It will be used to test the backend api with frontend
6. Create a chatgpt like ui, where we have thread/message