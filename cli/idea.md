I want cli like this, it will be a python package
when developer install it will show one command pyagenity

These are the command should be available
1. pyagenity --help
2. pyagenity api
This will start API server in dev mode using uvicorn nothing else
3. pyagenity run
This will start the uvicorn server and react application, so user can check the code and agent
4. pyagenity build
This will build the docker image for the agent


No need to docker and docker compose need to do using this library, that developer responsibility

# React TechStack For Prototype
- React
- Language: JavaScript
- React Router
- Redux
- Axios
- Shadcn/UI
- TanStack Query
- Tailwind CSS

Make the ui same as chatgpt
